DB_HOST=localhost
DB_USER=rdm_user
DB_PASS=password
DB_NAME=rdm_db

# Instrucciones:
# 1. Copie este archivo a .env
# 2. Modifique los valores de arriba con sus credenciales de base de datos reales.
# 3. Asegúrese de que su servidor web/PHP pueda acceder a estas variables de entorno.
#    Esto puede requerir configuración adicional del servidor o usar una librería PHP como phpdotenv
#    en un archivo de arranque (bootstrap) de la aplicación (no implementado aquí).
# 4. NUNCA suba su archivo .env real a un repositorio de código.
#    Asegúrese de que .env esté en su archivo .gitignore.
